# Sika — R8 rules.
#
# Nothing here yet. Room's generated code and the parser are both plain Kotlin that R8
# handles without help. Add a rule only when a release build actually breaks, and write
# down which class and why — an unexplained -keep is a rule nobody can ever remove.
