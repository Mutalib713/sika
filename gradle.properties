org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
# 8 logical cores, 15.7 GB RAM. Do NOT set this to 1 — that cap belonged to the old
# Avast laptop and is not needed on this machine.
org.gradle.workers.max=6
org.gradle.java.home=C\:\\Program Files\\Android\\Android Studio\\jbr
android.useAndroidX=true
android.nonTransitiveRClass=true
# No android.builtInKotlin / android.newDsl flags here. Those belong to AGP 9, and this
# project deliberately runs AGP 8 so it needs neither — see the root build.gradle.kts for
# the full reasoning.
# No custom truststore. Avast is not installed here, so Java's own cacerts work. If a
# download ever fails with "PKIX path validation failed", the cause is something else —
# do not re-add systemProp truststore lines by reflex.
